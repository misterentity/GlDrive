# v3.10.127 — stale race listings no longer revoke completed transfers

## Log review scope

Reviewed the retained September 20–21 logs on September 21, 2026 (America/Vancouver), focusing on the v3.10.126 session that started at 09:15:39. That covered 16,234 lines through 19:00: **0 Error, 0 Fatal, 27 Warning**. There were 0 `SelfConnectMode` exceptions, 0 pending-reply discards and 0 login-limit failures, so v3.10.126's fix held for ~10 hours of live racing. The same exceptions occurred hundreds of times before 09:15.

All 27 warnings have known external causes (see Remaining risks):

| Warning | Count | Cause |
| --- | --- | --- |
| Download parked — `T:\` not mounted | 13 | Destination drive absent |
| SYN mount/remount failed (`Timed out trying to connect!`) | 13 | SYN FTP host unreachable since 2026-08-13 |
| Previous session left a running marker | 1 | Expected after the v3.10.126 task-based update |

Racing was healthy. There were 854 completed FXP transfers, and every race that started logged `Race Complete`. Windows Application log has no GlDrive error or crash events in the last two days.

## Defect found below warning level

Across 854 completions, the INF log showed **55 `FXP dupe-skip` events for files this client had uploaded itself 11–44 s earlier** ("It was uploaded by entity (0m 18s ago)"). A dupe-skip ties up a transfer slot and both logins for a Relay RETR/STOR round trip, and it delivers nothing.

Timeline for one example, `the.split.up.s01e02…r65` (14:07):

| Time | Event |
| --- | --- |
| :06.338 | zephyr `LIST` returns 8 files (r65 not yet there) |
| :09.772 | `FXP complete: …r65` — ownership recorded for zephyr |
| :11.288 | The :06 listing is **applied** (it waited on the slower source listing in the same batch) |
| :42.357 | r65 re-sent → `FXP dupe-skip (Relay)` |

**Root cause.** A scan listing is a snapshot from when `LIST` ran, but `SpreadJob.ProcessFiles` applies it later, after the whole scan batch finishes. `FileOwnershipReconciler.Prune` (v3.10.107) treats "owned before, absent now" as a deletion. Any completion recorded between the snapshot and its application was therefore revoked. The file was re-selected and marked in-flight at once. `Observe` skips in-flight files, so the 5–13 correct listings that followed could not restore ownership before the pointless re-send ended in a dupe-skip. A snapshot taken mid-upload can cause the same revocation through `Observe`'s size check, because it shows a partial size.

**Evidence.** A script over the log found the ordering `LIST returned < FXP complete < listing applied` for **55 of 55** dupe-skips. Overall, 168 zephyr completions landed in that window. The rest were re-observed before being re-selected.

## Fix

- `SpreadJob` keeps a monotonic ownership epoch under `_ownershipLock`. Each confirmed transfer stamps `(file, dest)` with a new epoch. Each scan task captures the current epoch **before** its `LIST` runs.
- `ProcessFiles` treats a file whose ownership on that site was confirmed after the listing's epoch as in-flight for `Observe`. That listing's size cannot demote it. The same predicate goes to `Prune`, so the file's absence from that listing does not revoke it.
- Later listings (captured after the stamp) apply normally. A genuine deletion is still pruned on the next cycle, which preserves the v3.10.107 phantom-volume behavior.

## Validation

| Check | Result |
| --- | --- |
| New regression tests (red before fix: API absent) | 2 reconciler tests + 1 wiring test |
| Full regression | **1,374 passed**, 0 failed, 0 skipped |
| Solution build | 0 warnings, 0 errors |
| Mutants (each diff-checked, each compiles) | 4/4 caught: prune guard removed; Observe guard removed; comparison inverted; epoch stamp made non-advancing |
| Native FTPS/WinFsp harness (`tools/ReleaseCheck`) | 22/22 assertions passed; fixture stopped and its temp dir removed |

The harness does not exercise a remote glftpd race. Production verification is below.

## Deployment

See the section appended after release.

## Remaining risks

- **SYN** FTP host is still unreachable (13 timeouts this session). This is external; the remount loop backs off correctly.
- **`T:\`** is not mounted, so Backrooms.2026 BLURAY stays parked (no retries consumed). This is external.
- Working set was 230 MB five minutes after the v3.10.126 start and 346 MB about 10 h later, per the heartbeat. Two points do not establish a leak. The value resets with this deploy; trend it on the next sweep.
- Noise, not errors: `SiteStatsCollector` logs the full `SITE USER` body (about 10 lines, 118 times in 10 h, ~7% of lines), and FiSH decrypt diagnostics log about 1,080 INF lines. Neither is harmful, but both speed up log rollover. They are left for a separate change.
