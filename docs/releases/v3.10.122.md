# GlDrive v3.10.122

## Log review and root causes

Reviewed the September 14–16 logs (through 19:00 PT), focusing on everything since v3.10.121 started at 09:17:07 (15,579 lines).

| Log date | Lines | Error | Fatal | Warning |
| --- | ---: | ---: | ---: | ---: |
| September 14 | 30,814 | 0 | 0 | 71 |
| September 15 | 36,101 | 0 | 0 | 78 |
| September 16 | 35,231 | 0 | 0 | 60 |

Warning classification (September 16): 23 SYN mount/remount timeouts (host unreachable since August 13), 22 missing-T: download parks, 12 FXP failure header/cause lines (superbnc→zephyr `425 Can't build data connection` and one `550` on a source relocation), one followed relocation, and the stale-heartbeat and duplicate-launch lines from the 06:45 startup that v3.10.121 already corrected. All are external or previously handled.

Health checks since v3.10.121 started: 595 FXP completes against 2 failures, 34 races complete with 0 failed jobs, 0 borrow timeouts, 6 poisoned discards (4 are the scheduled Relay re-probe, 1 is a pending data reply, 1 is a source-attributed FXP fault), the AI agent run finished `status=ok`, and no extractor failures. Private memory was 259 MB and handle count 1,205 after 10 hours. No leak trend.

The frequent destination-only scans (every 3–5 s during a race, with a full scan every 20 s) are normal race pacing. Every race observed ended `Spread race complete`.

**One defect fixed: incomplete skip attribution.** 35 auto-races were skipped with "No viable destination". The 19 tv-sports skips logged only `(superbnc.xxxxx.tw: download-only)`. zephyr had already been removed from the participant list for having no `[tv-sports]` section, and that reason, the one an operator can act on, was dropped from the message. `SpreadManager.TryAutoRaceInternalAsync` now builds the reason through `CandidatePredicates.DescribeNoViableDestination`, which appends missing-section and rules/metadata denials. The skip decision itself is unchanged: it was correct.

## Validation

| Check | Result |
| --- | --- |
| New `NoViableDestinationReasonTests` | Failed to compile before the change; 4/4 passed after |
| Wiring mutant (reverted call site) | Caught: 1 failed |
| Release build | 0 warnings, 0 errors |
| Full regression suite | 1,344 passed, 0 failed/skipped |

## Remaining risks

- SYN FTP host is still unreachable, so its races and mount keep failing. This needs a server-side fix.
- `T:\` is not mounted, so the Backrooms BLURAY download parks every hour. The operator needs to mount the drive or change `downloads.localPath`.
- zephyr has no tv-sports section, so tv-sports announces can never race. This is a configuration decision; this release only makes it visible.
- superbnc→zephyr 425 refusals (about 1% of transfers) are server-side. Existing retry policies handle them.
- The change only affects a log/UI string. There are no schema or persisted-state changes. Roll back with the v3.10.121 installer if needed.
