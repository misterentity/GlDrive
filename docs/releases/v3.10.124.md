# GlDrive v3.10.124

## Log review and root causes

Reviewed all retained daily application logs for September 16–18, including the three September 17 size rolls, through approximately 09:02 PT on September 18. Historical standalone crash dumps predate this review window (the newest is July 20).

| Log | Lines | Error | Fatal | Warning |
| --- | ---: | ---: | ---: | ---: |
| September 16 | 37,891 | 0 | 0 | 77 |
| September 17, all four files | 296,976 | 1 | 0 | 240 |
| September 18, through 09:02 | 37,911 | 0 | 0 | 34 |

The one Error was a download permanently failing at September 17 07:37:13 with `Pool exhausted: all connections discarded and new connections failed`. Its destination drive, T:, was absent. The manager listed the remote directory before checking its local destination; the network outage therefore consumed ordinary retries before the existing volume-parking handler could run. The fix checks destination availability before FTP access, preserving the queued item and retry budget with the existing five-minute-to-one-hour recheck schedule. Cancellation is checked first, and the existing handler still covers a drive disappearing later during a transfer.

The outage also caused DNS failures, unreachable LAN connections, IRC reconnects, notification poll failures, exhausted pools and failed spread scans. Version 3.10.123 already added transport-specific backoff and attribution. That installed version was confirmed by both the authenticated control API and current heartbeat. No additional reconnect storm was observed in the September 18 log. Through 09:02, there were 1,283 successful FXP files, 11 failed attempts, 62 completed races and no failed races.

Other warnings were remote 425 data-connection refusals, connection resets, release paths relocated by the source server (followed by existing recovery), repeated SYN mount failures, and absent T: download parking. Search indexes also report partial results when remote directories reject LIST (16–19 failed directories in the September 18 samples); the AI agent's 04:01 run finished `status=ok`. No new application crash or native failure was identified in the reviewed daily logs. September 17's unclean-session warning follows the machine outage already investigated for v3.10.123; a running marker alone cannot establish an application crash.

## Validation before publication

| Check | Result |
| --- | --- |
| New download lifecycle regression, before fix | Both cases failed: retry consumed, or item became Failed |
| New regression, after fix | 2 passed; covers retry counts 0 and 3, restart/recheck and persisted state |
| Release solution build, NuGet advisories treated as errors | Passed, 0 warnings, 0 errors |
| Full regression suite | 1,368 passed, 0 failed, 0 skipped |
| Dependency audit, including transitive packages | No known vulnerable packages reported by configured sources |
| Disposable loopback FTPS/WinFsp harness | All 21 assertions passed |
| WPF smoke render | 15 fresh screenshots; Downloads and download Settings visually inspected |
| Installed baseline API smoke | Eight route/authentication/method checks passed; two of three servers connected |

Native checks exercised real GnuTLS transfers, notification detection/deduplication, media range reads, shared handles, disk-buffer spill, dirty rename, volume flush, mounted Windows create/write/read/rename/delete, and rejected-NOOP recovery/shutdown. The fixture uses a disposable local directory and its own certificate trust store.

Local evidence is retained under `.local-archive/release-3.10.124/` (gitignored). Production API credentials and private signing keys are not included in this report.

## Release and production verification

- Version **3.10.124**, release commit `f2b3cb4b6aef1e38035eb10ceffa4e02f1c80500`, pushed to master. [GitHub CI](https://github.com/misterentity/GlDrive/actions/runs/35366715643) passed against that commit.
- `installer/release.ps1` exited 0, repeated all 1,368 tests successfully, built the self-contained application and Inno installer, and published [v3.10.124](https://github.com/misterentity/GlDrive/releases/tag/v3.10.124) at **09:13:32 PT on September 18** with installer, ZIP, checksum manifest and RSA signature.
- Independently downloaded all four published assets. Verified the manifest signature against the public key embedded in the app and checked both packages against the manifest, local build and GitHub asset digests:
  - Installer SHA-256: `dfaca6905c1b78ed4bb7032b7c9f4c89002ead2178e60c948a0b8524126ee149`
  - Update ZIP SHA-256: `b601901acbc0adeac8c43632fc38181e68a9ba550249a84af65410fb5d1c9795`
- With no active races, selected **Update** in the installed application's tray menu at 09:14:39. The app verified its download signature and integrity, handed the update to the standard SYSTEM task at 09:14:43, and the watchdog relaunched it at 09:15:07. Startup confirmed installation at **09:15:08**. All seven GnuTLS crash-guard reflection checks resolved.
- All **1,149 installed files** match the published ZIP byte-for-byte. The production API, heartbeat and desktop version label independently identify **3.10.124.0**.
- The automated production monitor sampled every 30 seconds through **09:20:10**, completing five minutes on the new version. Heartbeats remained current; memory settled near 239 MB. zephyr connected immediately; superbnc had one initial handshake timeout, then remounted successfully at 09:16:26. Both remained connected thereafter; SYN remained unavailable as before.
- Repeated all eight API route/authentication/method checks on the installed release: passed. No Error or Fatal log entries and no matching Windows Application crash events during the observation window. No race ran during this window; native transfer behavior was exercised by the disposable FTPS/WinFsp harness, not a production race.
- Extended observation through **09:23:27 PT**: 234 log lines, **0 Error, 0 Fatal, 9 Warning** (four SYN attempts, the recovered FTP handshake timeout, three subsequently recovered IRC invitation warnings, and the expected missing-drive park). The pending IRC invitation retry succeeded at 09:21:14, channels rejoined, and encrypted announcements resumed. No production race ran; announces in this interval had no eligible destination section. Final API checks still passed, the heartbeat was 19 seconds old, and working set was 245 MB.
- Inspected the installed Downloads screen and retried the historical failed item at **09:22:41**. The production log confirms it parked immediately for missing T:, and persisted state now reads **Queued**, `Waiting for drive T:\ — destination volume is not mounted`, with retry count unchanged at **3**. This validates the fix on the affected production item, including an exhausted retry budget. The download itself remains blocked until its destination is available.

## Remaining risks and rollback

- T: is still absent. Downloads targeting it cannot complete until the drive is restored or an intended replacement path is supplied. The affected item was explicitly retried and is now parked safely; the fix does not silently revive other failed downloads.
- SYN's FTP endpoint remains unreachable. Two other configured servers are connected. Restoring SYN requires network/server repair or an updated endpoint.
- Remote 425 refusals, connection resets and source relocation remain possible; existing retry and relocation handling remain in place. Loopback smoke testing does not reproduce a remote BNC's CPSV behavior or constitute an extended live soak.
- Search results can be incomplete when remote directories reject listings. Existing unextractable archive sets remain skipped until their files change.
- Inno Setup emitted its existing warning about an administrative installer writing per-user HKCU settings; installation under a different administrator account may affect that account's autostart settings. The production update used the ZIP/task path.
- Missing destination-section mappings can still make an auto-race infeasible. They are configuration choices, not changed automatically.
- No configuration schema or stored-data migration. Roll back using the v3.10.123 installer if needed; that version retains the destination-check ordering defect.
