# v3.10.128 — preserve useful diagnostic history

## Log review and root causes

Reviewed the retained September 20–22 application logs on September 22, 2026
(America/Vancouver), including exception stacks and Information-level failures.
The September 22 snapshot through approximately 09:02 contained 39,253 lines,
38,249 Information events, 28 Warnings, and no Error or Fatal events.

- The earlier FluentFTP `SelfConnectMode` failures are confined to sessions before
  v3.10.126. They have not recurred in the current v3.10.127 session.
- No duplicate skips were recorded on September 22; more than 1,800 FXP transfers
  completed. The ownership-epoch correction in v3.10.127 continues to hold.
- SYN still times out connecting to its configured FTP endpoint. It accounts for
  nine of today's warnings; its retry loop backs off. The other two FTP servers
  are connected. This is an unresolved external endpoint failure.
- Nineteen warnings park two downloads because their destination drive `T:` is
  absent. Retry budgets are preserved. Windows has a remembered but unavailable
  mapping to `\\zephyr\tv`. Its SMB TCP port is reachable, but accessing the share
  fails and reconnecting with `net use` requests authentication (then exits 1223
  without credentials). Valid Windows share credentials are required; no
  alternative destination or credentials were guessed. A separate five-second
  TCP probe to SYN also timed out, corroborating the application's diagnosis.
- Two quarantines follow a direct CPSV-PASV capability probe, with Relay fallback;
  races continue successfully. Section/rule exclusions are expected configuration
  decisions. Stats ACL denials fall through the existing candidate chain.
- No GlDrive error/crash events were found in the Windows Application log for
  the preceding two days. Retained May–July crash dumps are historical evidence,
  not a current crash. The live heartbeat reported 261 MB working set after
  nearly 12 hours, versus 242 MB at the previous release's 45-minute checkpoint.

The actionable logging defect is volume: approximately 32,600 routine scan events
account for over 80% of today's Information events. Successful FiSH decryption
adds over 1,000 diagnostics, while repeated stats probes log account response
bodies. These events accelerate size-based rolling and obscure failure evidence.
A later captured snapshot through 09:13 contained 39,321 Information events;
34,968 (88.93%) match the diagnostic families changed by this release. That is a
retrospective estimate of event reduction, not a measured production rate.

## Changes

- Move per-snapshot scan start/list/result/application details to Debug. Keep
  transfer outcomes, fallback/failure explanations, contention summaries, and
  Warning/Error events at their existing levels.
- Move successful FiSH message diagnostics to Debug; retain decryption failures,
  recovery, and key-transition diagnostics. Skip diagnostic string computation
  when Debug is disabled.
- Move routine stats polling/results to Debug and remove raw SITE/LIST response
  bodies from logs entirely. Stats parsing and dashboard updates are unchanged.
- Bump the application and README version to 3.10.128. There is no configuration
  schema or persisted-data migration.

To investigate snapshot ordering or successful decryption, choose Debug in
Settings → Diagnostics, then return to Information after collecting evidence.
No existing log files are deleted by this change.

## Pre-release validation

| Check | Result |
| --- | --- |
| Baseline solution Release build | Passed, 0 warnings/errors |
| Baseline regression | 1,374 passed, 0 failed/skipped |
| Changed solution Release build | Passed, 0 warnings/errors |
| Changed full regression | 1,374 passed, 0 failed/skipped |
| Existing FiSH diagnostic privacy coverage | Passed within full suite, including Debug diagnostics |
| Native FTPS/WinFsp harness | 22 assertions passed |
| Dependency audit, including transitives | No known vulnerable packages reported by configured sources |
| WPF render smoke | 15 fresh renders; Notifications and Diagnostics visually inspected |
| Installed baseline API smoke | Eight route/authentication/method checks passed |

Native coverage includes 800 commands without session re-login, notification
detection/deduplication, real transfer integrity, bounded media reads, shared
handles, disk spill, dirty rename, volume flush, Windows mounted-file operations,
negative-NOOP recovery, and shutdown. Its isolated loopback fixture was stopped.
The UI renderer ran read-only with credential access disabled. These checks do
not establish a long remote BNC soak or exercise every interactive UI flow.

Local evidence is under `.release-tools/verification-20260922/` (gitignored).

## Deployment and observation

- Release commit `7e7e64d7e5e8300c078b8eabe5bc5c4033a0e652` was pushed to master.
  [GitHub CI](https://github.com/misterentity/GlDrive/actions/runs/35752182140)
  passed on that exact commit.
- `installer/release.ps1` exited 0 after repeating all 1,374 tests, publishing the
  self-contained build, building the installer, signing the checksum manifest,
  and publishing [v3.10.128](https://github.com/misterentity/GlDrive/releases/tag/v3.10.128)
  at **09:13:37 PT**. All four release assets are present.
- Independently downloaded the installer, ZIP, checksum manifest and signature.
  The manifest signature verifies against the application's embedded public key;
  both package hashes match the manifest, local artifacts and GitHub digests.
  - Installer SHA-256: `bda957565190772bb4dd4d7128fc56aec1d912983ebf1f489b295494fac61aee`.
  - ZIP SHA-256: `d220241cbd9369e7abe628b0cdb246236f4e20d8485c25fd5846eb75fb9f3e95`.
- The application's own scheduled updater detected the release at **09:17:19**,
  verified the signature/hash, and handed it to the existing SYSTEM update task
  at **09:17:23**. The app was idle before the update. No custom staging or manual
  file replacement was used.
- The watchdog relaunched GlDrive at **09:17:47** (PID 82488); startup confirmed
  **3.10.128.0** at **09:17:48** and reconciled the installed update at 09:17:49.
  The process version, API and heartbeat agree. **All 1,149 installed files**
  match the downloaded ZIP byte-for-byte by SHA-256.
- All seven native TLS guard members resolved; both available FTP servers and
  all three configured IRC connections recovered. Eight post-update API checks
  passed. The task's `0xFFFFFFFF` result is its existing deliberate process-kill
  exit behavior; completion logs and installed hashes confirm installation.
- Desktop automation could not find the tray-only GlDrive process as an
  accessible window. UI smoke evidence consists of the 15 release-build renders,
  not a claim of interactive production UI coverage.
- **Five-minute observation passed:** 11 samples from **09:17:57–09:22:57 PT**
  consistently reported v3.10.128.0, PID 82488, both available FTP servers
  connected, and heartbeats no older than 9.1 seconds. Final working set was
  195 MB. The monitor recorded one expected connection refusal during updater
  downtime before the new session; none during the completed observation.
- Log review through **09:23:29 PT** found **107 Information, 8 Warning,
  0 Error, 0 Fatal** events. All eight warnings are accounted for: four SYN
  mount/remount failures and four missing-T: parks. No `SelfConnectMode` failure
  or Windows Application GlDrive error/crash event appeared.
- Routine scan Information messages, successful FiSH Information diagnostics,
  and raw stats bodies are absent. **No production race ran after restart** in
  this window, so this does not measure scan-volume reduction under live race
  load. The native harness establishes transfer/filesystem smoke coverage; the
  88.93% event reduction estimate comes from replaying the pre-release log.
- Repeated all eight API smoke checks at **09:23:32 PT**: passed. SYN and T:
  remain the explicitly unresolved external dependencies; the application is
  stable with those services degraded.

## Remaining risks and rollback

- SYN and T: remain unavailable pending FTP endpoint recovery and valid SMB
  credentials (or an explicitly chosen replacement destination).
  Publishing this diagnostic change does not resolve those external failures.
- Debug logs intentionally remain verbose; enable them only while diagnosing.
- Automatic approval review rejected cleanup of the disposable fixture directory
  (`%TEMP%/gldrive-ftps-check-enzathm8`) as "blocked by policy". Its server process
  is stopped; the synthetic fixture files remain on disk.
- A short observation cannot prove multi-day stability, remote BNC behavior, or
  the absence of a memory leak. Retention improvement is workload-dependent.
- Inno Setup repeats its pre-existing warning that an administrative installer
  writes per-user HKCU settings. Using a different administrator account can
  affect that account's autostart setting. This deployment used the ZIP updater.
- Rollback is the previous v3.10.127 installer; it restores verbose logging and
  raw stats-body diagnostics, with no data migration required.
