# GlDrive v3.10.117

## Log review and root causes

Reviewed all retained application logs, September 12–14, 2026, through approximately 09:01 PT on September 14. The installed version was v3.10.116 throughout. Historical crash dumps predate this window (latest July 20); Windows Application events 1000/1001/1026 contained no GlDrive crash in the last three days.

| Day | Lines | Error | Fatal | Warning |
| --- | ---: | ---: | ---: | ---: |
| September 12 | 22,470 | 0 | 0 | 38 |
| September 13 | 30,713 | 0 | 0 | 43 |
| September 14, partial | 19,586 | 0 | 0 | 17 |

All 98 warnings were classified: 53 SYN remount failures; 17 transfer failure headers plus 17 corresponding cause lines (10 STOR 425, four RETR 425, three RETR 550); two source relocation notices; nine notification poll failures (eight LIST 425 rejections and one control-reply timeout). The three RETR 550 failures belonged to two releases that the source relocated; the existing relocation handling followed them. Failed direct-route probes are quarantined and fall back to Relay as designed. No unattributed poisoned discards were found.

The September 12 11:23:26 notification timeout left an outstanding data reply. The pool safely quarantined it, but could only name the generic pending-reply condition. Tracing this call identified three related client defects:

- `NewReleaseMonitor` called the raw listing APIs, bypassing `FtpOperations.ListDirectory` and its explicit connection quarantine and cause attribution. Both root/category listings now use that shared implementation. Clean final LIST rejections still preserve reusable connections.
- The outer loop treated every `OperationCanceledException` as shutdown. An internal data TCP/TLS deadline could therefore permanently stop polling. Only cancellation of the monitor's own token now exits. This permanent-stop risk was reproduced in a regression test; the retained timeout was a `TimeoutException`, so the logs do not prove a permanent stop occurred in production.
- Category failures were logged only at Debug and then treated as a successful poll. Successful categories now continue updating, failed snapshots are retained, and a single contextual exception per incomplete cycle reaches the existing warning/backoff policy. Shutdown propagates immediately. First-time recovery seeds a category without announcing its historical contents.

## Validation before release

| Check | Result |
| --- | --- |
| Release solution build with NuGet vulnerability warnings as errors | Passed, zero warnings/errors |
| Full regression suite | 1,303 passed, zero failed/skipped |
| New recovery tests | Six cases: deadline retries/backoff/reset; category transport/deadline recovery and deduplication; shutdown; partial-cycle backoff; initial recovery seeding |
| Regression sensitivity | Restoring the original unfiltered cancellation catch failed the recovery test (one poll instead of four); restoring the fix returned all 1,303 tests to green |
| Native FTPS/WinFsp smoke harness | 21 assertions passed, including three new notification seed/detect/dedup checks, transfer integrity, media ranges, disk buffering, shared handles, rename/flush, mounted create/write/read/rename/delete, rejected NOOP and verified recovery |
| WPF smoke render | Exit 0; 15 views generated; notification dashboard visually inspected with v3.10.117 stamp |
| Dependency audit | No vulnerable direct/transitive packages reported by configured sources |
| Diff whitespace check | Passed |

Local evidence is retained under `.local-archive/release-3.10.117/` (build, TRX, regression and native logs); UI renders are under `%AppData%/GlDrive/screenshots/`. Native tests use a disposable loopback server and isolated trust store.

## Remaining risks and rollback

- SYN's FTP endpoint remains unreachable. It is disconnected in the authenticated production status API; the retry ladder continues at five-minute intervals. Its network/server owner must restore reachability. No server configuration was disabled to conceal the failure.
- Intermittent remote 425 data-connection refusals persist. The client retries and backs off, but client logs cannot establish the server-side cause. Existing race connection hygiene and source relocation behavior remain covered by regression tests.
- Restart rediscovered two pre-existing incomplete watched RAR sets: each directory contains the initial `.rar` but no continuation volumes, with source timestamps from April/August. SharpCompress reports a missing volume and the UnRAR fallback exits 3. One directory also contains an existing extracted video. Six startup warnings describe these two attempts and their abandonment until the volume set changes. Completing these archives requires the missing source volumes; no user media was removed or redownloaded during this review.
- A repeatedly failing notification category now activates the existing longer poll interval for the whole monitor. This can delay healthy-category notifications, while preserving their processing and avoiding a hot retry loop. Persistent failures use the existing first-three-warning/then-Debug policy.
- Before deployment the v3.10.116 heartbeat showed approximately 396 MB working set at 59.5 hours uptime. This is a bounded observation, not proof of leak-free long-term operation.
- Native smoke tests do not reproduce a remote glftpd BNC's CPSV stack. Internal cancellation/recovery is fault-injected in regression tests. Production observation cannot prove all possible failures resolved.
- Inno Setup compiled successfully with its existing admin-install/HKCU startup-entry warning. Installing with a different administrator account can apply that optional startup entry to that account; the ZIP updater used by this installation does not rewrite it.
- Automatic approval review blocked removal of the disposable FTPS fixture directory (`gldrive-ftps-check-u035e69b` under the system temporary directory). Its server process was stopped. Only generated test data, a temporary self-signed fixture key/certificate, and isolated trust data remain there.
- Roll back using the v3.10.116 installer if notification recovery regresses. This release changes no persisted schema or credentials.

## Deployment and production verification

- Source commit and release tag: `416c5841782510a0f9f3b097a27c097bb9447133`.
- [GitHub CI run 34866678653](https://github.com/misterentity/GlDrive/actions/runs/34866678653) passed in 1m51s: restore, Release solution build, regression tests, dependency audit.
- `powershell -NoProfile -File installer/release.ps1` exited 0. It verified the bundled WinFsp pin, passed all 1,303 tests again, published the application, compiled the installer, created the update ZIP, and signed the checksums.
- [v3.10.117 release](https://github.com/misterentity/GlDrive/releases/tag/v3.10.117) published September 14 at 09:13:04 PT (16:13:04 UTC). All four assets uploaded. The tag resolves to the tested source commit.
- Both public binary assets were downloaded and their SHA-256 hashes verified against the public manifest. Its RSA signature verified against the updater's embedded public key.

| Artifact | SHA-256 |
| --- | --- |
| `GlDriveSetup-v3.10.117.exe` | `9d836627489f8155efdba6cec6e215a1a9e0f950df297790e910ff604582c3f9` |
| `GlDrive-v3.10.117-win-x64.zip` | `a14122115c0a2e39f5270e14541bd6e19d82bda0159082cbfa45579b7e49f7e6` |

Before upgrade, authenticated `/`, `/status`, `/sections`, `/races`, and `/history` returned HTTP 200. Unauthenticated status returned 401; an unknown route returned 404; POST to the read-only status route returned 405. Two servers were connected, SYN was disconnected, and no race was active.

The installed client detected the release at 09:30:59 PT, verified the signature and ZIP hash, and handed the update to the SYSTEM task at 09:31:03. The watchdog relaunched PID 46140 from `C:/Program Files/GlDrive/GlDrive.exe` at 09:31:26; the application confirmed v3.10.117 installed at 09:31:27. The cleanup task removed 1,148 old files with zero remaining.

The first post-upgrade API/heartbeat sample at 09:31:52 reported v3.10.117.0 and both previously available servers connected. One polling sample during restart received connection refused, followed by recovery on the next 30-second sample. Installed `GlDrive.exe` and `GlDrive.dll` hashes exactly match their entries in the downloaded, verified ZIP. All five read-only API probes and the 401/404/405 negative probes passed again. Both notification monitors successfully seeded from live CPSV listings (zero and 12 categories).

The observation completed at 09:36:53 PT: 11 API/heartbeat samples over five minutes all reported v3.10.117.0 and the same two connected servers. Heartbeats were at most 25 seconds old; final working set was 196 MB. No race ran during this window, so production transfer throughput was not exercised; the native transfer smoke harness passed before release.

From restart at 09:31:26 through 09:36:53, 137 timestamped log events contained zero Error, zero Fatal, and ten Warning entries: six for the two incomplete archive sets and four for SYN's initial connection failure/retry ladder. No notification poll failure occurred. There were no GlDrive Windows Application crash events after deployment. The running marker exists, the updating marker and pending handoff are absent, and both scheduled tasks are Ready. Raw monitoring samples and the final counts are retained in `production-health.jsonl` and `production-summary.json` under the local evidence directory.

Release publication, installation, integrity verification, regression/smoke testing, and bounded production monitoring are complete. Production remains degraded by SYN reachability and the incomplete source archives described above; the intermittent remote 425 condition also remains an external risk. This short observation does not replace a long-running soak or demonstrate that unavailable external services were repaired.
