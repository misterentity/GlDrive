# v3.10.132 — complete races that already reached their sole receiver

## Log review and root causes

Reviewed retained September 22–24 logs on September 24, 2026 (America/Vancouver).
Snapshots contain 46,359, 1,834 and 6,795 events respectively, ending at
10:02:05 PT on September 24. There are **0 Error and 0 Fatal** events; warnings
and recovery messages were also reviewed. Initial directory metadata misleadingly
showed today's log ending at midnight; reading a snapshot confirmed current
logging and transfers. No new logging defect was found.

- **Incorrect race failure:** two September 23 announces originated on zephyr,
  the sole receiving site. Discovery found the release there; the other peer,
  superbnc, lacked it and was download-only. The early two-site minimum check
  reported `Need 2+ servers` before the existing already-present no-op handling.
  This produced false configuration failures in logs, history and telemetry.
- **SYN:** mount/remount timeouts remain an external outage. A fresh five-second
  TCP probe timed out. Both other FTP servers remain connected.
- **T: downloads:** the destination drive is still absent. Two downloads remain
  parked without spending retry budgets. Restoring the intended drive/share
  requires operator credentials or infrastructure repair. Windows still remembers
  `\\zephyr\tv`; one attempt to restore that exact mapping with the current Windows
  credentials failed with `The specified network password is not correct`.
  No destination or credential was changed.
- **September 24 transfer warnings:** two RETR 550 failures followed a source
  release relocation; the engine followed the new path and completed that race.
  Two remote connection resets recovered through retry. Each transfer warning
  also has a job-level warning, accounting for eight warning events.
- **Historical September 22–23 warnings:** source relocations/resets recovered;
  the notification TLS deadline and Plex transient were covered by the previous
  release review. The watchdog notices and rejected second instances align with
  the documented v3.10.131 deployment/recovery exercise. No new GL Drive Windows
  Application crash event was found in the preceding day.
  A fresh read-only Plex check passed account resource discovery, configured and
  loopback identity/library requests (HTTP 200), and the app's own connection probe.

## Changes

- After source discovery, classify the sole-receiver case as the existing
  already-present completion when every remaining participant is download-only.
  Require positive discovery of a receiving site; do not infer presence from an
  announce alone. No transfer or failure event is emitted for this no-op.
- Keep missing receiving sites, mapping/blacklist failures, never-source
  destinations, and topologies with no receiving sites on their existing paths.
- Six regression cases and a native FTPS lifecycle reproduction added. The
  reproduction failed against the previous code, then passed after the fix.
- Version 3.10.132; no configuration or persisted-data migration. README and
  changelog updated, including the previously omitted v3.10.131 entry.

## Pre-release validation

| Check | Result |
| --- | --- |
| Baseline regression | 1,384 passed |
| Full regression with new cases | 1,390 passed, 0 failed, 0 skipped |
| Solution Release build, warnings as errors | 0 warnings, 0 errors |
| Native FTPS/WinFsp smoke | 26 assertions passed; sole-receiver reproduction failed before fix |
| Dependency audit, including transitives | No known vulnerable packages from configured sources |
| WPF render smoke | 15 fresh renders, renderer exit 0; Search and Diagnostics visually checked |
| Installed baseline API smoke | Eight route/authentication/method checks passed; v3.10.131.0, 2/3 servers connected |

Native smoke covers 800 commands on one TLS session, real race discovery,
search, notification deduplication, upload/download integrity, bounded media
reads, disk buffering, shared handles, rename/flush, mounted Windows file
operations, negative-NOOP recovery and shutdown. Evidence is retained locally
under `.release-tools/verification-20260924/` (gitignored).
The fixture server was stopped. Automatic approval review rejected temporary
fixture-directory cleanup as "blocked by policy"; its synthetic data remains in TEMP.
The installed UI accessibility tree confirms v3.10.131, two FTP connections and
three IRC connections. Desktop capture was unavailable and tab selection could
not be verified, so this is not a complete interactive UI test.

## Deployment

- Release commit `29ce96624a02ce809a21096914cf0120742faa6b` pushed to master.
  [GitHub CI](https://github.com/misterentity/GlDrive/actions/runs/36032275361)
  passed on that commit, including Release build, all tests and dependency audit.
- `installer/release.ps1` exited 0 and published
  [v3.10.132](https://github.com/misterentity/GlDrive/releases/tag/v3.10.132)
  at **10:14:20 PT**, with installer, update ZIP, manifest and RSA signature.
  The pipeline repeated all 1,390 tests and verified the bundled WinFsp MSI pin.
- All four published assets were independently downloaded. The checksum signature
  verifies against the public key embedded in the app, and package hashes agree
  between downloaded assets, local builds, the signed manifest and GitHub digests.
  The release tag points at the exact CI-tested commit.
  - Installer SHA-256: `065148700269ed082983bc3263ebccf511176149c1341964845ba0fd0b120627`.
  - ZIP SHA-256: `9d0c1ea850315be3691c05353a18d80a7e425b094ddb18534f04414dd0e34753`.
- Pre-deployment API smoke passed again. The deployment helper waits for no
  active transfers (the same criterion as the updater), rechecks immediately
  before graceful Restart Manager shutdown, and relaunches with job breakaway.
  A disposable child verified breakaway before production was touched.
  The first recheck refused shutdown when another transfer began; production was
  untouched. The final helper additionally waited for completed file work or
  initial discovery, avoiding a scheduling gap in the middle of a partial race.
- Graceful shutdown completed at **10:22:59**, and a verified breakaway launch
  started PID 67264 at **10:23:04**. The normal startup updater verified the
  manifest/signature and handed installation to the existing SYSTEM task at
  **10:23:39**. Version 3.10.132 was confirmed installed at **10:24:03**.
- All **1,149 installed files** match the independently downloaded ZIP. Eight
  post-update API checks passed. Eleven healthy samples from **10:24:36 to
  10:29:36** report PID 51220, both available FTP connections, and heartbeats
  no older than 3.4 seconds. Logs contain 181 Information, 8 Warning, 0 Error
  and 0 Fatal events through 10:29:56. Warnings concern SYN and T:.
- **Follow-up found during production smoke:** Information events exposed a
  separate account-stats bug: standard LIST attempted the BNC's unusable passive
  endpoint, then cached that transport failure as a six-hour access denial.
  This is fixed and validated in [v3.10.133](v3.10.133.md), which supersedes this
  release and completes the same review. This report does not claim all findings
  were resolved by v3.10.132 alone.

## Remaining risks and rollback

- SYN and T: remain unavailable external dependencies; production is degraded.
- Existing race semantics treat a discovered release directory as already
  present. This change does not add a file-completeness audit or change filling
  behavior for those directories.
- Native smoke uses isolated loopback FTPS, not a remote glftpd BNC CPSV soak.
  Render checks do not cover every interactive flow or long-term stability.
- A launcher job that forbids breakaway can still terminate both app and
  watchdog; use the normal desktop/logon launch or verified job breakaway.
- Inno Setup retains its existing administrative-install/HKCU warning: running
  installation as another administrator can affect that account's user settings.
- Rollback uses the v3.10.131 installer; no data migration is required.
